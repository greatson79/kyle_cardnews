---
name: sns-cardnews-verify
description: >
  [TLDR] sns-cardnews 스위트의 독립 검증 하위 스킬. 작성·빌드를 수행한 스킬과 분리된
  검증 전담자로서, 최종 산출물(slides.md·PNG·캡션·카톡)을 성경 인용 정확성·복음 비트
  생존·규격·플레이스홀더·과장 표현 5축으로 감사해 PASS/FAIL 판정문을 산출한다. 이 스킬을
  통과하지 못한 산출물은 완성으로 인정되지 않는다.
  [Triggers] 대표 스킬(sns-cardnews)이 빌드 완료 후 6단계에서 반드시 호출한다(생략 불가).
  재캡처·수정 후에도 변경분에 대해 재호출된다. 사용자가 직접 호출하지 않는다.
  [Detailed Methodology] 5축 검증: ①성경 인용 대조 — slides.md·캡션의 모든 성경 인용을
  장절 단위로 실제 성경 본문과 대조한다(웹 검색 또는 로컬 성경 자료). 대조 불가 시
  "검증 불가"로 기재하지 FAIL도 PASS도 주지 않는다 — 검증 실패를 통과로 채우는 것 금지.
  ②복음 비트 생존 — 복음 적용 장(기본 6장·브리프가 지정한 장)에 그리스도를 가리키는
  복음 전환 문장이 실재하는가. 도덕 교훈만 있으면 FAIL. ③규격 실측 — PNG 10개 존재·
  해상도 1080×1350(도구로 실측: sips/file 등)·HTML .slide 10개. ④플레이스홀더·금지선 —
  템플릿 변수 잔존·이모지 아이콘·로고 텍스트 대체 스캔. ⑤과장·거짓 확신 — 근거 없는
  단정("이 말씀대로 하면 반드시 형통")·번영복음적 표현·원고에 없는 사실 주장 검출.
  판정 규칙: 각 축 PASS/FAIL/검증불가 + 근거(실제 산출물의 행·파일 인용 — 산출물 안의
  자기 준수 선언문을 근거로 쓰는 순환논증 금지). 종합 = 전 축 PASS일 때만 PASS.
  verify-report.md로 저장하고 대표 스킬에 반환한다.
disable-model-invocation: true
---

# Verifier (하위 스킬 · 독립 검증 전담)

작성자와 검증자는 분리된다. 이 스킬은 산출물을 **만들지 않는다** — 판정만 한다.
수정 지시가 필요하면 "무엇이 왜 실패했는지"를 기재해 대표 스킬이 담당 하위 스킬에
되돌리게 한다.

## 5축 검증

| 축 | 방법 | FAIL 조건 |
|----|------|----------|
| 1. 성경 인용 | 장절 단위 원문 대조(웹/로컬 자료) | 오인용·존재하지 않는 구절·왜곡 |
| 2. 복음 비트 | 복음 적용 장 문장 실재 확인 | 그리스도 지시 문장 부재·도덕 교훈만 |
| 3. 규격 | 도구 실측(개수·해상도) | PNG≠10·해상도≠1080×1350(×스케일 배수) |
| 4. 플레이스홀더·금지선 | 전 산출물 스캔 | `{변수}` 잔존·이모지 아이콘·텍스트 로고 |
| 5. 과장·거짓 확신 | 문장 단위 검토 | 근거 없는 단정·원고에 없는 사실 주장 |

## 판정 규율

- **근거는 실제 콘텐츠 행만** — 산출물이 "금지선을 준수했다"고 적은 자기 선언문은
  준수의 증거가 아니다(순환논증 금지).
- **검증 불가 ≠ PASS** — 대조할 수 없었던 항목은 "검증 불가 + 사유 + 훑은 범위"로
  정직 기재한다. 검증 불가 항목이 있으면 종합 판정에 그 사실을 명시한다.
- **부재 판정은 방법 명시** — "문제 없음"이라 쓰려면 무엇을 어떻게 훑었는지 쓴다.

## 출력: verify-report.md

```markdown
# 검증 보고
- 대상: (파일 목록 + 검증 시점)
- 축별 판정: 1~5 각 PASS/FAIL/검증불가 + 근거(파일:행 인용)
- 종합: PASS / FAIL(재작업 지시 대상 스킬 명시)
```
