---
name: sns-cardnews-context
description: >
  [TLDR] sns-cardnews 스위트의 컨텍스트 추출 하위 스킬. 설교 원고 전문·부분 요약·음성
  전사·메모 등 어떤 형태의 설교 자료에서도 카드뉴스 제작에 필요한 구조화 필드(제목·본문·
  CMT·FCF·HP·예화)를 자동 추출해 sermon-context.md로 산출한다. 사람이 수동으로 요약해
  입력하던 작업을 대체하는 AI 에이전트다.
  [Triggers] 대표 스킬(sns-cardnews)이 입력 분류에서 "구조화 컨텍스트 없음"으로 판정했을
  때만 호출된다. 사용자가 직접 호출하지 않는다.
  [Detailed Methodology] ①자료 전체를 읽고 설교의 논지 전개를 파악한다(부분 발췌로 판단
  금지). ②추출 필드: 설교 제목(없으면 콘텐츠용 제목 제안), 성경 본문(책·장·절 정확 표기),
  CMT(본문의 중심 진리 1~2문장), FCF(본문이 다루는 인간의 타락 상태·문제), HP(그리스도
  안에서의 복음적 해답 — 도덕적 교훈이 아니라 복음), 핵심 예화 1개(있으면). ③각 필드에
  근거 위치(원고의 해당 대목 인용 1줄)를 함께 기록한다 — 근거 없이 그럴듯하게 지어내는
  것 금지. 원고에 HP가 실제로 없으면 "원고에 명시적 복음 전환 부재"로 정직 기재하고 추출
  실패로 보고한다(대표 스킬이 사용자에게 질문하게 된다). ④산출 = sermon-context.md
  (SKILL.md 입력 스키마 형식) + 필드별 근거 목록.
disable-model-invocation: true
---

# Context Extractor (하위 스킬)

## 추출 원칙

1. **전문 정독 후 추출** — 서두·결론만 보고 CMT를 추정하지 않는다.
2. **근거 동반 의무** — 모든 필드에 원고 인용 1줄을 붙인다. 인용할 수 없는 필드는
   추출하지 않는다(빈 필드 + 사유 기재).
3. **HP는 창작 금지** — 원고에 복음 전환이 없으면 만들어 넣지 않는다. "부재"로 보고해
   대표 스킬이 설교자에게 확인하게 한다. (스킬이 설교자의 신학을 대신 쓰면 안 된다.)
4. **본문 표기 정확성** — 성경 책명·장·절은 원고 표기를 따르되, 원고가 모호하면
   본문 인용 구절로 역추적해 확정하고 [역추적] 표기를 남긴다.

## 출력 형식

```markdown
# Sermon Context
- 설교 제목: ...
- 콘텐츠용 제목(제안): ...
- 성경 본문: 막 10:35-45
- CMT: ...
- FCF: ...
- HP: ...
- 예화: ...

## 추출 근거
- CMT ← "원고 인용..." (위치)
- FCF ← "..."
- HP ← "..." (또는: 부재 — 명시적 복음 전환을 찾지 못함)
```
